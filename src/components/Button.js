
export const Button = ({children}) => {
    return (
        <button className="w-64 text-xl rounded-md h-12 bg-gradient-to-r from-teal-400 to-blue-500 hover:from-pink-500 hover:to-orange-500 ...">{children}</button>
      )
}

